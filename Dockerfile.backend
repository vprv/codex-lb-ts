FROM node:22-bookworm-slim

WORKDIR /app

COPY package.json package-lock.json tsconfig.json ./
RUN npm ci

COPY src ./src

ENV HOST=0.0.0.0
ENV PORT=2455
ENV DB_PATH=/data/codex-lm-ts.db

EXPOSE 2455 1455

CMD ["npm", "run", "start"]
